# Gere uma variável do tipo "lista" que contenha a sequência numérica de 1000 à 1, decrescente:

# lista_numeros_decrescentes = [int(n) for n in range(1000, 0, -1)]
# print(lista_numeros_decrescentes)

lista_numeros_decrescentes2 = list(range(1000, 0, -1))
print(lista_numeros_decrescentes2)